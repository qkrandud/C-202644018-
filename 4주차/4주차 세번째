#include <stdio.h>

int fact(int n) {
    if(n == 1) {
        return 1;
    }
    else {
        printf("%d\n",n);
        n--;
        return fact(n);
    }
}
int main() {
    int num;
    printf("숫자를 입력하세요:");
    scanf("%d",&num);

    printf("%d",fact(num));

    return 0;
}
